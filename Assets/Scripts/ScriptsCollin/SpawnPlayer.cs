using UnityEngine;
using System.Collections;

public class SpawnPlayer : MonoBehaviour
{

    private GameObject selectedcharacter;
    private GameObject persistentObj;
    // Use this for initialization
    void Start()
    {
        //persistentObj = GameObject.FindGameObjectWithTag("PersistentObject");

        //selectedcharacter = persistentObj.GetComponent<SelectedCharacter>().selChar;

        //Instantiate(selectedcharacter, this.transform.position, Quaternion.identity);
        
    }
}
